# 参与贡献

感谢你改进简普水印工作室。请优先提交范围清晰的 issue 或 pull request，并保持“完全离线、不覆盖原图、默认设置简单”的产品原则。

## 开发流程

1. Fork 仓库并从 `main` 创建功能分支。
2. 使用 Node.js 24 和 npm 11 安装依赖：`npm install`。
3. 修改后运行 `npm run check` 和 `npm run test:e2e`。
4. Pull request 中说明用户问题、实现方式、验证命令；界面变化请附截图。

不要提交用户图片、安装包、密钥或版权不明的 Logo/字体。新增内置素材时必须同时提供明确的授权来源。
